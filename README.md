# Macro Regime Engine v9.1 — Layout-Perfect Command Center

This build is a layout-first consolidation rebuild. It keeps the universal market functions, gauges, global sessions, active cause logic, selectable tiles, score/change %, instrument map, order-flow proxy, and instrument pressure, but reorganizes the main dashboard into a clean command-center layout.

Upload these root files to GitHub for Streamlit Cloud:

- `app.py`
- `requirements.txt`
- `README.md`
- `.gitignore`

Main file path for Streamlit: `app.py`.
